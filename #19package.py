#package is a container of related modules
#package is like a directory with related files
#we have to create a new directory and in the directory we keep the close related modules
# to convert the durectory into a package we need to add a special file in it
# the special file is __init__.py


#importing a package we write
#import the entire module
#import nameofpackage.modulename
#using it we do
#nameofpackage.modulename.functuonname


#another method is 
#from nameofpackage.modulename import fuctionname, functionname2
#accessing the functions we use onlu function 
#functionname()

#or import the module from the package
# from packagename import modulename
#modulename.functionname()